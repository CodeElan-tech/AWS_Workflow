name: Object-Detection-Model
conda_env: conda.yaml

entry_points:
  main:
    command: "python main.py"
  
  run_all:
    command: |
      python main.py && python try.py && mlflow run . -e run_ui
  
  run_ui:
    command: |
      mlflow ui --host 127.0.0.1 --port 5000
