name: Object-Detection-Model

docker_env:
  image: my_object_detection_image  

entry_points:
  main:
    command: "python main.py"
  
  run_all:
    command: |
      python main.py && python try.py 
  
